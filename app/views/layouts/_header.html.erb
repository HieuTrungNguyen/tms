<nav class="navbar navbar-default navbar-fixed-top" role="nagivation">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to t(".logo"), root_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav navbar-right">
      <% if logged_in? %>
        <li class="dropdown">
          <a href="" class="dropdown-toggle" data-toggle="dropdown">
            <%= t ".account" %> <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to t(".profile"), show_path(current_user) %></li>
            <li><%= link_to t(".settings"), edit_path(current_user) %></li>
            <li class="divider"></li>
            <li><%= link_to t(".logout"), logout_path, method: :delete %></li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to t(".help"), "#" %></li>
        <li><%= link_to t("form.login"), login_path %></li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>
