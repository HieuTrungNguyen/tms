<div class="row">
  <h1 class="subjects-title"><%= t ".subject_title" %></h1>
  <div>
    <%= render @subjects %>
  </div>
  <div class="paginate text-center">
    <%= paginate @subjects, theme: "twitter-bootstrap-3" %>
  </div>
</div>
